#include <stdio.h>
/**
 *main- prints the alphabet in lowercase, followed by a new line.
 *Return: 0
*/
int main(void)

{
	char alphabet;

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(alphabet);
	return (0);
}
