#include "main.h"

/**
 * mul - that multiplies two integers.
 * @a:fonction int mul (int a)
 * @b:fonction int mul (int b)
 * Return: Always 0.
 */
int mul(int a, int b)

{
	int c;

	c = (a * b);
	return (c);
}
